连接1.0是非持久连接，1.1是长连接
1.1增加range头域，1.0不支持断点续传，
1.1增加host域名，可支持多个不同ip访问一个主机
1.1新增head，delete请求
1.1缓存新增etag，if-none-match

http1.1和http2
数据流：
采取二进制传输：二进制相对1.1采用文本来说，编译速度更快
多路复用，服务端和客户端可以同时发送多个请求和回应，而且不用按顺序发送
服务端推送，服务端可以向客户端推送必要的静态资源，这样可以减少一些延迟时间
header压缩 ，引入头部信息压缩机制，头部信息使用gzip和compress压缩再发送，
客户端和服务端同时维护一张信息表，所有字段会加入这个表，生成索引，只用发送索引号，提高速度